import org.testng.annotations.Test;

public class MethodOverloading {

    @Test
    public void methodOverloadingTest() {
        Select autoSelect = new Select();

      // autoSelect.selectBy(index: 1);
      //  autoSelect.selectBy(visibleText: "Volvo");

    }
}
